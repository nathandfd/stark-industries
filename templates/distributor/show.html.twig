{% extends 'base.html.twig' %}

{% block title %}Distributor{% endblock %}

{% block body %}
    <h1>Distributor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ distributor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ distributor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('distributor_index') }}">back to list</a>

    <a href="{{ path('distributor_edit', {'id': distributor.id}) }}">edit</a>

    {{ include('distributor/_delete_form.html.twig') }}
{% endblock %}
